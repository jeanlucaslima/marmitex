export const LOCALE = {
  lang: "pt", // this will be added to the <html> tag
  langTag: ["pt-BR"],
  // Standard value for text direction is left to right, if needed, change it below
  textDirection: "ltr"
} as const;